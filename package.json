{
  "name": "webpack-cpp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "webpack",
    "build:docker": "docker run --rm -v $(pwd):/src -t apiaryio/emcc npm run build",
    "build:docker:win": "docker run --rm -v %CD%:/src -t apiaryio/emcc npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cpp-wasm-loader": "^0.9.0",
    "html-webpack-plugin": "^5.5.0",
    "webpack": "^4.29.6",
    "webpack-cli": "^3.3.0"
  }
}
